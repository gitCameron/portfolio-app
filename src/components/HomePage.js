import React from 'react';

// state-less functional component
const HomePage = () => (
    <div>
        <h1>Welcome</h1>
        <p>This is the home page of the application.  Take a look around.</p>
    </div>
);

export default HomePage;